# virc environment configuration
# Copy to .env and fill in values: cp .env.example .env

# --- Required ---

# Secret key for signing JWT tokens (used by virc-files)
JWT_SECRET=change-me-to-a-random-string

# LiveKit API credentials (must match config/livekit/config.yaml keys section)
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=devsecret

# --- Optional (defaults shown) ---

# MySQL root password for the history database
MYSQL_ROOT_PASSWORD=changeme

# Ergo REST API endpoint (internal Docker network address)
# ERGO_API=http://ergo:8089

# Bearer token for Ergo HTTP API (must match config/ergo/ircd.yaml bearer-tokens)
# ERGO_API_TOKEN=sTcmero5do9UZFqJ8HYSpgnXUckvTLtnbY1uRYeU1Ak

# LiveKit server URL (internal Docker network address)
# LIVEKIT_URL=ws://livekit:7880

# Port for the virc-files HTTP server
# PORT=8080

# Path to the virc.json config file (relative to virc-files working dir)
# CONFIG_PATH=config/virc.json

# LiveKit URL returned to clients for voice (set to LAN IP for LAN access)
# LIVEKIT_CLIENT_URL=ws://localhost:7880

# Server name displayed in the client
# SERVER_NAME=
