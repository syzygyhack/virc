# Normalize line endings for all text files
* text=auto

# Force LF for shell scripts (required for bash/sh)
*.sh text eol=lf

# Force LF for Docker and config files processed inside Linux containers
Dockerfile text eol=lf
Caddyfile text eol=lf
*.yaml text eol=lf
*.yml text eol=lf

# Force CRLF for Windows-native scripts
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
