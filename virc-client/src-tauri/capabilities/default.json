{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Main chat window capabilities — least-privilege set for the primary UI",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "opener:default",
    "core:event:default",
    "core:window:default"
  ]
}
